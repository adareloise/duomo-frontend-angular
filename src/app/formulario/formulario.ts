import { FormControl, FormGroup } from '@angular/forms';

export class Formulario {
    
    id: number;
    nombre: string;
    apellido: string;
    email: string;
    fechaNacimiento: Date;
    telefono: string;
    region: string;
    comuna: string;
}